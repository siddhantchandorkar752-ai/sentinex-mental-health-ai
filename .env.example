# Offline, deterministic, synthetic outputs; safe default for development and deployment.
SENTINEX_MODE=demo

# Opt in only when the optional model dependencies and pinned model weights are available:
# SENTINEX_MODE=transformers
